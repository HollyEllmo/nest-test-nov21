// Proto file is located at: libs/proto/src/audit.proto
// It should be loaded via @grpc/proto-loader at runtime

export const AUDIT_PACKAGE_NAME = 'audit';
export const AUDIT_SERVICE_NAME = 'AuditService';
export const AUDIT_PROTO_PATH = __dirname + '/audit.proto';
